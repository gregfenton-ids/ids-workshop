/**
 * Browser no-op stub for reflect-metadata.
 * reflect-metadata is only needed at runtime for NestJS/TypeORM decorator
 * metadata reflection, which never runs in the browser.
 */

// No-op — the Reflect API is not needed in the browser for our use case.
